---
phase: 01-error-foundation-and-lexer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/eaml-errors/src/lib.rs
  - crates/eaml-errors/src/codes.rs
  - crates/eaml-errors/src/severity.rs
  - crates/eaml-errors/src/diagnostic.rs
  - crates/eaml-errors/src/render.rs
  - crates/eaml-errors/Cargo.toml
autonomous: true
requirements: [ERR-01, ERR-02, ERR-03, ERR-04]

must_haves:
  truths:
    - "All 38 error codes from spec/ERRORS.md are defined as enum variants"
    - "A Diagnostic struct carries code, message, span, severity, and hints"
    - "Diagnostics render with codespan-reporting colored output"
    - "Multiple errors accumulate without aborting on first error"
  artifacts:
    - path: "crates/eaml-errors/src/codes.rs"
      provides: "ErrorCode enum with all 38 compiler codes"
      contains: "pub enum ErrorCode"
    - path: "crates/eaml-errors/src/severity.rs"
      provides: "Severity enum"
      contains: "pub enum Severity"
    - path: "crates/eaml-errors/src/diagnostic.rs"
      provides: "Diagnostic struct and DiagnosticCollector"
      contains: "pub struct Diagnostic"
    - path: "crates/eaml-errors/src/render.rs"
      provides: "codespan-reporting integration"
      contains: "to_codespan"
  key_links:
    - from: "crates/eaml-errors/src/diagnostic.rs"
      to: "crates/eaml-errors/src/codes.rs"
      via: "Diagnostic.code field is ErrorCode"
      pattern: "code:\\s*ErrorCode"
    - from: "crates/eaml-errors/src/render.rs"
      to: "codespan_reporting::diagnostic::Diagnostic"
      via: "to_codespan conversion method"
      pattern: "codespan_reporting"
---

<objective>
Build the eaml-errors crate: shared error infrastructure for all compiler phases.

Purpose: This is the dependency root -- every other crate depends on eaml-errors for error codes, diagnostics, and rendering. Must be complete before the lexer can emit errors.
Output: Working eaml-errors crate with ErrorCode enum (38 codes), Diagnostic struct, DiagnosticCollector, and codespan-reporting rendering.
</objective>

<execution_context>
@/Users/dunnock/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dunnock/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-error-foundation-and-lexer/01-CONTEXT.md
@.planning/phases/01-error-foundation-and-lexer/01-RESEARCH.md
@spec/ERRORS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ErrorCode enum, Severity enum, and Diagnostic struct</name>
  <files>
    crates/eaml-errors/src/codes.rs
    crates/eaml-errors/src/severity.rs
    crates/eaml-errors/src/diagnostic.rs
    crates/eaml-errors/src/lib.rs
    crates/eaml-errors/Cargo.toml
  </files>
  <read_first>
    spec/ERRORS.md
    crates/eaml-errors/src/lib.rs
    crates/eaml-errors/Cargo.toml
    .planning/phases/01-error-foundation-and-lexer/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test: ErrorCode::Syn042.to_string() returns "SYN042"
    - Test: ErrorCode::Res001.to_string() returns "RES001"
    - Test: All 38 error codes from spec/ERRORS.md exist as enum variants (Syn042, Syn043, Syn044, Syn045, Syn046, Syn050, Syn060, Syn061, Syn080, Syn081, Syn082, Syn083, Syn090, Sem010, Sem020, Sem025, Sem030, Sem035, Sem040, Sem050, Sem060, Sem061, Sem070, Typ001, Typ002, Typ003, Typ010, Typ030, Typ031, Typ032, Typ040, Cap001, Cap002, Cap010, Cap020, Pyb001, Pyb010, Res001) plus 4 new lexer codes (Syn001, Syn002, Syn003, Syn004)
    - Test: Severity has Fatal, Error, Warning variants
    - Test: Diagnostic can be created with code, message, span (Range<usize>), severity, label, and hints (Vec<String>)
    - Test: Diagnostic with no hints has empty hints vec
    - Test: ErrorCode implements Debug, Clone, Copy, PartialEq, Eq, Hash
  </behavior>
  <action>
    Create three source files in crates/eaml-errors/src/:

    **codes.rs**: Define `pub enum ErrorCode` with all 38 error codes from spec/ERRORS.md plus 4 new lexer-specific codes from the reserved SYN001-039 range:
    - SYN lexer (new): Syn001 (unexpected character), Syn002 (unterminated string literal), Syn003 (unterminated block comment), Syn004 (invalid escape sequence)
    - SYN lexer (spec-defined): Syn042, Syn043, Syn044, Syn045, Syn046
    - SYN parser: Syn050, Syn060, Syn061, Syn080, Syn081, Syn082, Syn083, Syn090
    - SEM: Sem010, Sem020, Sem025, Sem030, Sem035, Sem040, Sem050, Sem060, Sem061, Sem070
    - TYP: Typ001, Typ002, Typ003, Typ010, Typ030, Typ031, Typ032, Typ040
    - CAP: Cap001, Cap002, Cap010, Cap020
    - PYB: Pyb001, Pyb010
    - RES: Res001

    Implement `std::fmt::Display` for ErrorCode that formats as uppercase prefix + zero-padded 3-digit number (e.g., `Syn042` -> "SYN042", `Res001` -> "RES001").

    Derive: `Debug, Clone, Copy, PartialEq, Eq, Hash`.

    Add a `pub fn prefix(&self) -> &'static str` method returning "SYN", "SEM", "TYP", "CAP", "PYB", or "RES".

    Add a `pub fn number(&self) -> u16` method returning the numeric portion (e.g., Syn042 -> 42).

    **severity.rs**: Define `pub enum Severity { Fatal, Error, Warning }` with `Debug, Clone, Copy, PartialEq, Eq` derives.

    **diagnostic.rs**: Define:
    ```rust
    pub type Span = std::ops::Range<usize>;

    #[derive(Debug, Clone)]
    pub struct Diagnostic {
        pub code: ErrorCode,
        pub message: String,
        pub span: Span,
        pub severity: Severity,
        pub label: String,
        pub hints: Vec<String>,
    }
    ```

    Add a `Diagnostic::new(code, message, span, severity, label)` constructor that initializes hints to empty Vec.
    Add a `Diagnostic::with_hint(mut self, hint: impl Into<String>) -> Self` builder method.

    **lib.rs**: Re-export all public types:
    ```rust
    pub mod codes;
    pub mod severity;
    pub mod diagnostic;

    pub use codes::ErrorCode;
    pub use severity::Severity;
    pub use diagnostic::{Diagnostic, Span};
    ```

    No changes needed to Cargo.toml -- thiserror and codespan-reporting already listed.
  </action>
  <verify>
    <automated>cargo test -p eaml-errors</automated>
  </verify>
  <acceptance_criteria>
    - crates/eaml-errors/src/codes.rs contains `pub enum ErrorCode` with at least 42 variants (38 spec + 4 new)
    - crates/eaml-errors/src/codes.rs contains `impl std::fmt::Display for ErrorCode`
    - crates/eaml-errors/src/codes.rs contains `fn prefix(&self) -> &'static str`
    - crates/eaml-errors/src/severity.rs contains `pub enum Severity`
    - crates/eaml-errors/src/severity.rs contains `Fatal` and `Error` and `Warning`
    - crates/eaml-errors/src/diagnostic.rs contains `pub struct Diagnostic`
    - crates/eaml-errors/src/diagnostic.rs contains `pub type Span = std::ops::Range<usize>`
    - crates/eaml-errors/src/diagnostic.rs contains `fn with_hint`
    - crates/eaml-errors/src/lib.rs contains `pub use codes::ErrorCode`
    - `cargo test -p eaml-errors` exits 0
    - `cargo clippy -p eaml-errors -- -D warnings` exits 0
  </acceptance_criteria>
  <done>All 42 error codes defined with Display formatting, Severity enum has 3 variants, Diagnostic struct carries all required fields, all tests pass, clippy clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DiagnosticCollector and codespan-reporting rendering</name>
  <files>
    crates/eaml-errors/src/diagnostic.rs
    crates/eaml-errors/src/render.rs
    crates/eaml-errors/src/lib.rs
  </files>
  <read_first>
    crates/eaml-errors/src/diagnostic.rs
    crates/eaml-errors/src/codes.rs
    crates/eaml-errors/src/severity.rs
    crates/eaml-errors/src/lib.rs
    spec/ERRORS.md
    .planning/phases/01-error-foundation-and-lexer/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test: DiagnosticCollector::new(20) creates collector with max_errors=20
    - Test: collector.emit(error_diagnostic) adds to diagnostics vec
    - Test: collector.has_errors() returns true after emitting an Error-severity diagnostic
    - Test: collector.has_errors() returns false for Warning-only diagnostics
    - Test: After emitting 21 Error-severity diagnostics with max_errors=20, collector.overflow() returns true and only 20 are stored
    - Test: collector.diagnostics() returns slice of all collected diagnostics
    - Test: collector.error_count() returns count of Error-severity diagnostics
    - Test: to_codespan converts Diagnostic to codespan_reporting::diagnostic::Diagnostic with correct severity mapping (Fatal->Bug, Error->Error, Warning->Warning)
    - Test: to_codespan includes error code in the diagnostic code field (e.g., "SYN042")
    - Test: to_codespan includes primary label with span
    - Test: to_codespan includes hints as notes
    - Test: render_to_string produces output containing the error code and message
  </behavior>
  <action>
    **diagnostic.rs** -- Add DiagnosticCollector to the existing file:
    ```rust
    pub struct DiagnosticCollector {
        diagnostics: Vec<Diagnostic>,
        max_errors: usize,
        error_count: usize,
        overflow: bool,
    }
    ```

    Methods:
    - `pub fn new(max_errors: usize) -> Self` -- default max_errors is 20
    - `pub fn emit(&mut self, diag: Diagnostic)` -- if diag.severity is Error or Fatal, increment error_count. If error_count > max_errors, set overflow=true and return without storing. Otherwise push to vec.
    - `pub fn has_errors(&self) -> bool` -- error_count > 0
    - `pub fn overflow(&self) -> bool` -- self.overflow
    - `pub fn error_count(&self) -> usize` -- self.error_count
    - `pub fn diagnostics(&self) -> &[Diagnostic]` -- slice reference
    - `pub fn into_diagnostics(self) -> Vec<Diagnostic>` -- consume and return

    **render.rs** -- codespan-reporting integration:
    ```rust
    use codespan_reporting::diagnostic::{Diagnostic as CSDiagnostic, Label};
    use codespan_reporting::files::SimpleFiles;
    use codespan_reporting::term;
    use codespan_reporting::term::termcolor::{ColorChoice, StandardStream, Buffer};
    ```

    Functions:
    - `pub fn to_codespan(diag: &Diagnostic, file_id: usize) -> CSDiagnostic<usize>` -- Convert our Diagnostic to codespan-reporting's Diagnostic. Map severity: Fatal->Severity::Bug, Error->Severity::Error, Warning->Severity::Warning. Set `.with_code(diag.code.to_string())`, `.with_message(&diag.message)`, primary label at `diag.span` with `diag.label` text, and notes from `diag.hints`.
    - `pub fn render_diagnostics(files: &SimpleFiles<&str, &str>, diagnostics: &[Diagnostic])` -- Write to stderr using `StandardStream::stderr(ColorChoice::Auto)` and `term::emit` for each diagnostic.
    - `pub fn render_to_string(files: &SimpleFiles<&str, &str>, diagnostics: &[Diagnostic]) -> String` -- Render to a `Buffer` (no color) for testing purposes. Use `Buffer::no_color()` and `term::emit` into it, then convert to String.

    **lib.rs** -- Add re-exports:
    ```rust
    pub mod render;
    pub use diagnostic::DiagnosticCollector;
    ```
  </action>
  <verify>
    <automated>cargo test -p eaml-errors && cargo clippy -p eaml-errors -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - crates/eaml-errors/src/diagnostic.rs contains `pub struct DiagnosticCollector`
    - crates/eaml-errors/src/diagnostic.rs contains `fn emit(&mut self, diag: Diagnostic)`
    - crates/eaml-errors/src/diagnostic.rs contains `fn has_errors(&self) -> bool`
    - crates/eaml-errors/src/diagnostic.rs contains `fn overflow(&self) -> bool`
    - crates/eaml-errors/src/render.rs contains `pub fn to_codespan`
    - crates/eaml-errors/src/render.rs contains `pub fn render_diagnostics`
    - crates/eaml-errors/src/render.rs contains `pub fn render_to_string`
    - crates/eaml-errors/src/render.rs contains `codespan_reporting`
    - crates/eaml-errors/src/lib.rs contains `pub mod render`
    - crates/eaml-errors/src/lib.rs contains `pub use diagnostic::DiagnosticCollector`
    - `cargo test -p eaml-errors` exits 0
    - `cargo clippy -p eaml-errors -- -D warnings` exits 0
  </acceptance_criteria>
  <done>DiagnosticCollector accumulates up to max_errors then overflows, codespan-reporting renders colored diagnostics with error codes, render_to_string works for test assertions, all tests pass, clippy clean</done>
</task>

</tasks>

<verification>
cargo test -p eaml-errors && cargo clippy -p eaml-errors -- -D warnings
</verification>

<success_criteria>
- eaml-errors crate compiles and all tests pass
- ErrorCode enum has all 42 variants (38 spec + 4 new lexer codes)
- ErrorCode Display formats as "SYN042", "RES001", etc.
- Diagnostic struct carries code, message, span, severity, label, hints
- DiagnosticCollector accumulates errors with configurable max (default 20)
- DiagnosticCollector stops after max_errors and reports overflow
- codespan-reporting renders diagnostics with colored source snippets
- render_to_string enables test assertions on diagnostic output
- clippy clean with -D warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-error-foundation-and-lexer/01-01-SUMMARY.md`
</output>
